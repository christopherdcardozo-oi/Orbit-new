# deps
node_modules/

# builds
.next/
dist/
build/
.expo/
.expo-shared/

# env
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store

# editor
.vscode/
.idea/

# original per-repo clones kept for reference; not part of the monorepo
sources/

# per-project MCP server config may reference tokens via env; the file itself
# is safe to commit as long as no tokens are inlined, but until this repo has
# a remote it's simpler to keep it local.
.mcp.json



# Firebase native config (paths referenced by expo prebuild; contain
# secrets not appropriate for git — user drops them in locally per
# docs/native-build-setup.md).
apps/mobile/google-services.json
apps/mobile/GoogleService-Info.plist
# Expo prebuild output — regenerated from app.json + plugins; the
# source of truth is app.json, never these folders.
apps/mobile/ios/
apps/mobile/android/
supabase/.temp/
orbit_flyer.pdf
